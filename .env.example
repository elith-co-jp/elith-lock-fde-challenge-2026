# Safe local placeholders only. Do not commit real secrets.
VITE_APP_ENV=local
VITE_APP_NAME="ELITH LOCK FDE Challenge"
VITE_DEMO_DATA_SOURCE=static

# Optional: candidates may use this for local-only mock integrations.
VITE_MOCK_AI_ENDPOINT=http://localhost:8787/mock-ai
